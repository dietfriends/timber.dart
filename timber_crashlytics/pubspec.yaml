name: timber_crashlytics
description: A new Flutter project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  timber:
    path: ../timber
  firebase_crashlytics: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
